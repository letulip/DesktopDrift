# macOS
.DS_Store

# Claude local settings (keep launch.json shared, ignore machine-local overrides)
.claude/settings.local.json

items/raw

# Build output
dist/
dist-*/
dist*.zip

# Node
node_modules/

# Promo capture artifacts — large, regenerable binaries produced by tools/capture/
# (see docs/promo/steps/). Keep them out of git; render locally when a store needs one.
*.gif
*.mp4
